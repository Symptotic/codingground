#include <iostream>
#include <vector>

double smallest(const std::vector<double>& vec_in);
double greatest(const std::vector<double>& vec_in);
double mean(const std::vector<double>& vec_in);
double total(const std::vector<double>& vec_in);